export type Person = {
  firstName: string
  lastName: string
  email: string
  timeInVerzion: number
  country: string
}

export type Policy = {
  name: string
  description: string
  lastUpdated: string
  applied: string
}
